extends ../layout.pug

block content
  p Hello there 👋
  p #{email} has signed up on the platform.
  p You can find him on MongoDB in the Creators collection with this query:
  code {email: "#{email}"}
  if ambassadorEmail && abassadorName
    p L'ambassadeur du créateur est #{ambassadorName} (#{ambassadorEmail})
  else
   p Le créateur n'a pas d'ambassadeur.

