extends ../layout.pug

block content
  p Hi #{username} 👋
  p You have a new message from #{senderUsername} on Revolt. Here it is:
  br
  p(style="white-space: pre-wrap;") « #{message} »
  br
  a.btn.btn-primary(href=link target='_blank') Reply to #{senderUsername}
